# Runtime dependencies, bounded to the major versions this code is written
# against so a fresh install cannot silently pull a breaking release.
#
# pydantic, pydantic-settings, beautifulsoup4, requests and the `ollama`
# client were previously listed but are not imported anywhere: the models are
# plain dataclasses and Ollama is reached through langchain-community.
flask>=3.0,<4.0
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-ollama>=0.2,<1.0
faiss-cpu>=1.8,<2.0
python-dotenv>=1.0,<2.0
