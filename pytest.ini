[pytest]
# tests-live/ is excluded by default: it needs a running Ollama and a seeded
# index, and it loads the real langchain stack that tests/conftest.py stubs.
# Run it with `make test-live`.
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
