{
  "name": "ai-agent-discovery-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend tests for AI Agent Discovery",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
